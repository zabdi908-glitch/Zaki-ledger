node_modules/
dist/
__pycache__/
.env
.env.local
.env.*.local
*.log
.DS_Store
supabase/config.toml

# Historical repair rehearsal run products (hashes are recorded in
# rehearsal/EVIDENCE.md; frozen artifacts + freeze records + proofs are
# committed under repair-013-pre/artifacts/ when they are the recorded
# evidence). Per-run generated manifests/logs are not committed.
supabase/repair-013-pre/rehearsal/generated/
# Per-run private test dirs of the drift/failure suites (run products, not
# committed evidence — recorded evidence lives in artifacts/ top level and
# rehearsal/EVIDENCE.md).
supabase/repair-013-pre/artifacts/auth-tests/
supabase/repair-013-pre/artifacts/drift-tests/
